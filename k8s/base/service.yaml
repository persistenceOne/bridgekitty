apiVersion: v1
kind: Service
metadata:
  name: bridgekitty-backend
  labels:
    app: bridgekitty-backend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: bridgekitty-backend
